<Field @label={{@label}} @error={{@error}} as |id|>
  <select
    id={{id}}
    class="field-control"
    {{on "change" this.handleChange}}
    ...attributes
  >
    {{#each this._options key="value" as |opt|}}
      <option value={{opt.value}} selected={{opt.selected}}>
        {{opt.label}}
      </option>
    {{/each}}
  </select>
</Field>